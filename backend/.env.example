# Database (Supabase PostgreSQL)
# Use direct connection (port 5432) for migrations, pooler (port 6543) for runtime
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-1-eu-central-1.pooler.supabase.com:5432/postgres"

# YouTube Data API v3 key (https://console.cloud.google.com/)
YOUTUBE_API_KEY="AIza..."

# Groq API key for ingredient extraction (https://console.groq.com/)
GROQ_API_KEY="gsk_..."

# NextAuth secret (must match frontend AUTH_SECRET)
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret"

# Resend API key for email alerts (https://resend.com/) — optional
RESEND_API_KEY=""

# Email sender address for alerts — optional
ALERT_FROM_EMAIL="Kitvas <alerts@kitvas.com>"

# Internal SSE broadcast secret (any random string)
INTERNAL_BROADCAST_SECRET="your-broadcast-secret"

# Server port
PORT=4001

# Environment
ENVIRONMENT=development
